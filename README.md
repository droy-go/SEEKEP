# SEEKEP - لغة برمجة عربية عالية المستوى

<p align="center">
  <strong>Smart Expressive Efficient Knowledge Execution Platform</strong>
</p>

<p align="center">
  <img src="https://img.shields.io/badge/version-1.0.0-blue.svg" alt="Version">
  <img src="https://img.shields.io/badge/license-MIT-green.svg" alt="License">
  <img src="https://img.shields.io/badge/language-C-orange.svg" alt="Language">
</p>

---

## 📖 نظرة عامة

**SEEKEP** هي لغة برمجة عربية عالية المستوى، مفتوحة المصدر، مكتوبة من الصفر بلغة C. تم تصميمها لتكون:

- 🌟 **سهلة التعلم**: بناء جمل عربي بسيط وبديهي
- ⚡ **سريعة**: ترجمة إلى بايتكود وتنفيذ عبر جهاز افتراضي فعال
- 🔧 **قوية**: دعم كامل للبرمجة كائنية التوجه، والإغلاقات، والمزيد
- 📦 **مستقلة**: لا تعتمد على أي مكتبات خارجية
- 🌍 **عربية 100%**: جميع الكلمات المفتاحية باللغة العربية

---

## ✨ المميزات

### أنواع البيانات
- ✅ الأعداد الصحيحة (`صحيح`)
- ✅ الأعداد العشرية (`عشري`)
- ✅ النصوص (`نص`)
- ✅ القيم المنطقية (`منطقي`: صحيح/خطأ)
- ✅ القيم الفارغة (`فارغ`)
- ✅ القوائم (`قائمة`)
- ✅ القواميس (`قاموس`)
- ✅ الدوال (`دالة`)
- ✅ الأصناف (`صنف`)

### ميزات اللغة
- 🔤 كلمات مفتاحية عربية: `متغير`، `دالة`، `إذا`، `أثناء`، `لكل`، `صنف`، ...
- 🎯 البرمجة كائنية التوجه مع الوراثة
- 📎 الإغلاقات (Closures) والدوال المجهولة (Lambdas)
- 🔄 جمع القمامة التلقائي (Garbage Collection)
- 📚 مكتبة قياسية غنية
- 🛠️ أدوات تطوير متكاملة (REPL، مصحح أخطاء، ...)

---

## 🚀 البدء السريع

### متطلبات البناء
- مترجم C (gcc أو clang)
- make
- نظام Unix/Linux أو Windows مع MinGW

### البناء من المصدر

```bash
# استنساخ المستودع
git clone https://github.com/seekep/seekep.git
cd seekep

# بناء المشروع
make

# تثبيت على النظام (اختياري)
sudo make install
```

### تشغيل أول برنامج

```bash
# الوضع التفاعلي
./bin/seekep -i

# أو تشغيل ملف
./bin/seekep أمثلة/مرحبا.سكيب
```

---

## 📝 أمثلة

### مرحبا بالعالم
```seekep
اطبع("مرحباً بالعالم!")
```

### المتغيرات والعمليات
```seekep
متغير الاسم = "محمد"
متغير العمر = 25
متغير المجموع = 10 + 20 * 3

اطبع("الاسم: " + الاسم)
اطبع("المجموع: " + نص(المجموع))
```

### الدوال
```seekep
dالة تحية(الاسم) {
    أرجع "مرحباً، " + الاسم
}

اطبع(تحية("العالم"))

# دالة lambda
متغير مضاعف = دالة(س) => س * 2
اطبع(مضاعف(5))  # 10
```

### تدفق التحكم
```seekep
# عبارة إذا
متغير الدرجة = 85
إذا (الدرجة >= 90) {
    اطبع("ممتاز")
} وإلا إذا (الدرجة >= 70) {
    اطبع("جيد")
} وإلا {
    اطبع("يحتاج تحسين")
}

# حلقة أثناء
متغير عداد = 1
أثناء (عداد <= 5) {
    اطبع("العدد: " + نص(عداد))
    عداد = عداد + 1
}

# حلقة لكل
لكل (i في المدى(1، 6)) {
    اطبع("التكرار: " + نص(i))
}
```

### القوائم والقواميس
```seekep
# قائمة
متغير أرقام = [1، 2، 3، 4، 5]
أضف(أرقام، 6)
اطبع(أرقام[0])  # 1

# قاموس
متغير شخص = {
    "الاسم": "أحمد",
    "العمر": 30
}
اطبع(شخص["الاسم"])  # أحمد
```

### الأصناف
```seekep
صنف شخص {
    دالة init(الاسم، العمر) {
        هذا.الاسم = الاسم
        هذا.العمر = العمر
    }
    
    دالة تقديم() {
        اطبع("أنا " + هذا.الاسم)
    }
}

متغير أحمد = جديد شخص("أحمد"، 25)
أحمد.تقديم()
```

---

## 📚 الكلمات المفتاحية

| العربية | المعنى |
|---------|--------|
| `متغير` | تعريف متغير |
| `ثابت` | تعريف ثابت |
| `دالة` | تعريف دالة |
| `أرجع` | إرجاع قيمة |
| `إذا` | شرط |
| `وإلا` | else |
| `أثناء` | while loop |
| `لكل` | for loop |
| `في` | in (للتكرار) |
| `توقف` | break |
| `استمر` | continue |
| `صنف` | class |
| `جديد` | new |
| `هذا` | this |
| `صحيح` | true |
| `خطأ` | false |
| `فارغ` | null |
| `و` | and (&&) |
| `أو` | or (||) |
| `ليس` | not (!) |

---

## 🔧 الأدوات

### سطر الأوامر
```bash
# الوضع التفاعلي
seekep -i

# تشغيل ملف
seekep برنامج.سكيب

# ترجمة فقط
seekep -c برنامج.سكيب -o برنامج.skpbc

# طباعة AST
seekep -a برنامج.سكيب

# طباعة البايتكود
seekep -b برنامج.سكيب

# وضع التصحيح
seekep -d برنامج.سكيب
```

---

## 📦 المكتبة القياسية

### الرياضيات
- `جذر(س)` - الجذر التربيعي
- `أس(س، ص)` - الأس
- `جيب(س)`، `جيب_تام(س)`، `ظل(س)` - الدوال المثلثية
- `لوغاريتم(س)`، `لوغاريتم10(س)` - اللوغاريتمات
- `عشوائي()`، `عشوائي(من، إلى)` - الأرقام العشوائية
- `أصغر(...)`، `أكبر(...)` - الحد الأدنى والأقصى

### النصوص
- `نص(قيمة)` - التحويل إلى نص
- `كبير(نص)`، `صغير(نص)` - تحويل الحالة
- `تقليم(نص)` - إزالة المسافات
- `قسم(نص، فاصل)` - تقسيم النص
- `اربط(قائمة، فاصل)` - ربط النصوص
- `استبدل(نص، قديم، جديد)` - استبدال
- `يبدأ_بـ(نص، بادئة)`، `ينتهي_بـ(نص، لاحقة)` - التحقق

### القوائم والقواميس
- `أضف(قائمة، عنصر)` - إضافة عنصر
- `أدخل(قائمة، فهرس، عنصر)` - إدراج
- `اسحب(قائمة، فهرس)` - إزالة وإرجاع
- `احذف(قائمة/قاموس، فهرس/مفتاح)` - حذف
- `امسح(قائمة/قاموس)` - مسح
- `رتب(قائمة)` - ترتيب
- `اعكس(قائمة)` - عكس
- `المفاتيح(قاموس)`، `القيم(قاموس)` - استخراج

### الملفات
- `افتح(مسار، نمط)` - فتح ملف
- `اقرأ(مسار)` - قراءة ملف
- `اكتب(مسار، محتوى)` - كتابة ملف
- `موجود(مسار)` - التحقق من الوجود
- `أنشئ_مجلد(مسار)`، `احذف_مجلد(مسار)` - إدارة المجلدات
- `المحتويات(مسار)` - قائمة الملفات

---

## 🏗️ بنية المشروع

```
SEEKEP/
├── المصدر/           # ملفات المصدر
│   ├── seekep.h      # النواة والأنواع
│   ├── seekep.c      # تنفيذ النواة
│   ├── lexer.h/c     # المعجم اللغوي
│   ├── parser.h/c    # المحلل اللغوي
│   ├── compiler.h/c  # المترجم
│   ├── vm.h/c        # الجهاز الافتراضي
│   └── main.c        # نقطة الدخول
├── أمثلة/            # أمثلة البرامج
├── اختبارات/         # اختبارات اللغة
├── Makefile          # نظام البناء
└── README.md         # هذا الملف
```

---

## 🔬 بنية اللغة

### المراحل
1. **المعجم اللغوي (Lexer)**: تحويل النص إلى رموز (tokens)
2. **المحلل اللغوي (Parser)**: بناء شجرة البنية المجردة (AST)
3. **المترجم (Compiler)**: تحويل AST إلى بايتكود
4. **الجهاز الافتراضي (VM)**: تنفيذ البايتكود

### البايتكود
- 60+ كود عملية (opcode)
- مكدس-based تنفيذ
- جمع قمامة تلقائي
- إدارة ذاكرة فعالة

---

## 🤝 المساهمة

نرحب بالمساهمات! يمكنك المساهمة عن طريق:

1. 🐛 الإبلاغ عن الأخطاء
2. 💡 اقتراح ميزات جديدة
3. 🔧 إرسال طلبات السحب (Pull Requests)
4. 📖 تحسين الوثائق

---

## 📄 الترخيص

هذا المشروع مرخص بموجب [رخصة MIT](LICENSE).

---

## 🙏 الشكر

شكر خاص لكل من ساهم في تطوير هذه اللغة.

---

<p align="center">
  <strong>صنع بحب ❤️ للغة العربية</strong>
</p>

<p align="center">
  <a href="https://github.com/seekep/seekep">GitHub</a> •
  <a href="https://seekep.org">الموقع الرسمي</a> •
  <a href="https://docs.seekep.org">التوثيق</a>
</p>
